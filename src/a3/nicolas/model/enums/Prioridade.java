package a3.nicolas.model.enums;

/*
 * Enum que representa os niveis de prioridade de um chamado.
 *
 * Usamos enum quando queremos limitar as opcoes possiveis.
 * Assim, um chamado so pode ter prioridade BAIXA, MEDIA ou ALTA.
 */
public enum Prioridade {
    BAIXA,
    MEDIA,
    ALTA
}
